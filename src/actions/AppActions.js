import alt from '../alt';

class AppActions {

    updateRouteName(name) {
        return name;
    }

    updateTheme(name) {
        return name;
    }

}

export default alt.createActions(AppActions);